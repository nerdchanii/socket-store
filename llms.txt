# socket-store

socket-store is a small WebSocket-first topic state store. It accepts a
standard WebSocket instance, routes incoming messages by topic, and lets topic
handlers turn payloads into local snapshots.

Canonical docs:

- Public docs site:
  `https://nerdchanii.github.io/socket-store/`
- Overview, install, lifecycle, migration notes, and non-goals:
  `README.md`
- Public API contract, runtime semantics, type contracts, error behavior, and
  undecided behavior:
  `docs/public/api.md`
- Public connection status values, transitions, close/error semantics, and
  deferred reconnect boundaries:
  `docs/public/connection-status.md`
- Proposed opt-in reconnect configuration, retry/backoff semantics, close
  behavior, auth/session boundaries, advanced reconnect placement, and
  implementation limits:
  `docs/public/reconnect.md`
- Runnable WebSocket topic-store example with setup, expected message flow, and
  cleanup:
  `docs/public/example.md`
- Documentation rules for future agent-authored changes:
  `docs/style-guide.md`
- Machine-readable package context for agents:
  `docs/agents/agent-context.json`
- Agent-facing guide and MCP evaluation note:
  `docs/agents/guide.md`
- Maintainer branch and release operations:
  `docs/project/branch-policy.md`, `docs/project/release-policy.md`,
  `docs/project/release-runbook.md`

Public APIs:

- `SocketStore`
- `createMessageHandler`
- `SocketStoreError`
- `SocketStoreProtocol`
- Schema helper types exported from `src/types.ts`

Use the default protocol only for complete JSON WebSocket messages shaped like
`{ "key": "...", "data": ... }`. Use the custom protocol adapter documented in
`README.md` and `docs/public/api.md` when a server sends a different complete
message shape.

Examples:

- `examples/basic` is the canonical runnable browser and local WebSocket server
  example.
- `npm run example:typecheck` verifies the example client types.

Non-goals:

- socket-store is not a React hook package, RPC framework, CRDT engine,
  persistence layer, advanced reconnection manager, byte-level stream parser,
  MCP docs-query integration, or A2A protocol implementation.
- Connection status APIs are implemented as framework-agnostic `getStatus()`
  and `subscribeStatus(listener)`. Future reconnect work must remain explicit
  and opt-in.

Before changing docs, prefer source-backed claims from `src/`, tests, and the
runnable example. Mark behavior as unsupported or undecided when the source and
tests do not define it.
